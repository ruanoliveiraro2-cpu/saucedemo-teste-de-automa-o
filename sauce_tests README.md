# Automação de Testes - SauceDemo (Atividade Prática III)

Repositório criado para a entrega da Atividade Prática III, contendo os scripts automatizados de testes desenvolvidos em Python, PyTest e Playwright.

## Como Executar
1. Instale as dependências:
   ```bash
   pip install -r requirements.txt
   playwright install
   pytest -v
